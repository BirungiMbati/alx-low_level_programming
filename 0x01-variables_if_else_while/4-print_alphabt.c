#include <stdio.h>
/**
 * main - Prints alphabet in lowercase.
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'a') {
			if ((c != 'q' && c <= 'z')
					putchar(c);
				c++;
		}
	putchar('\n');
	return (0);
}
